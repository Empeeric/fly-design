<div class="pageInfo">
    <div class="container">
        <div class="eight columns pageTitle"><h1>{page.title}</h1></div>
        <div class="eight columns breadcrumb">
            <a href="/">{config.site.homepage}</a>
            {#crumbs}
            {^last} <a href="{url}">{title}</a> {:else} {title} {/last}
            {/crumbs}
        </div>
    </div>
</div>